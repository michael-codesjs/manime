schema: "./schema.json" # GraphQL types (input file)
generates:
  ./src/types/api.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
    config:
      fetcher: fetch